package page

type RegigisterSucessVM struct {
	Title    string
	Username string
}

templ RegisterSuccess(vm RegigisterSucessVM) {
	<h1 class="text-2xl text-center py-4">{ vm.Title }</h1>
	<div class="w-full flex justify-center">
		Thank you for registering { vm.Username }!
	</div>
}
