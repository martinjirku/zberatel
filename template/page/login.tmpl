{{- define "content" -}}

{{- $title := .Title -}}
{{- $content := .Content -}}

<h1 class="w-full flex align-middle text-center">{{$title}}</h1>
<div class="w-full flex justify-center">
    {{- template "login-form" $content -}}
</div>
{{- end -}}