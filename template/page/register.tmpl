{{- define "content" -}}

{{- $title := .Title -}}
{{- $content := .Content -}}
<div class="flex flex-col">
    <h1 class="w-full text-center pt-4 text-lg font-bold">{{$title}}</h1>
    <div class="w-full flex justify-center">
        {{- template "register-form" $content}}
    </div>
</div>
{{- end -}}