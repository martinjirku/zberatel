version: "2"
sql:
  - schema: "schema.sql"
    queries: "db/queries.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "ksuid"
            go_type: "jirku.sk/kulektor/ksuid.KSUID"
